{
  "name": "localsec-extension",
  "version": "1.0.0",
  "description": "Extension Google Chrome pour LocalSec Audit",
  "scripts": {
    "build": "esbuild popup-src.js --bundle --outfile=bundle.js"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}
